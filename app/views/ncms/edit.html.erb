<div id="layoutSidenav_content">
    <main>
        <div class="container-fluid">
            <h3 class="my-5">Editar NCM</h3>
            <%= link_to 'Mostrar', @ncm, class:"btn btn-sm btn-success mr-3 mb-2" %>
            <%= link_to 'Voltar', ncms_path, class:"btn btn-sm btn-success mb-2" %>
            
            <%= render 'form', ncm: @ncm %>
        </div>
    </main>
    <%= render 'shared/footer' %>
</div>